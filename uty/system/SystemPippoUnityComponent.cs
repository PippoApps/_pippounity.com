// This is a singleton that should be setup in the GameStart GameObject
// Provides application helpers that need to stay in a MonoBehaviour, i.e. Internet Check with delegates
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SystemPippoUnityComponent : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
